# 2026-07-28

## Day 0 — the root goes down

Started the rebuild. The old system (`totem`) died the way the post-mortem predicted: a model compacted its own context, re-summarized finished work as unfinished, and looped on it unsupervised until the runtime broke, the sessions got deleted, the providers broke, and four months of quota burned in days. No malice — just a model obeying prose rules with nothing to stop it.

So the founding decision is that the rules stop being prose. They become code that runs around the model. Stone and adze.

Today: drafted the plan, locked the four founding decisions (enforcement-is-code; runs-on-anything-best-on-ours; the eight carves are the spec; root at `/home/nvii/TOT3MIC`), and laid down this self-describing root so any future agent (or future me, post-compaction) can read `STATE.md` and know exactly where we are without back-explaining.

Open: haven't chosen the substrate yet (fork OpenCode middle-path vs rebuild on Aider vs greenfield). That's the next conversation.

Also applied the parked web search fix (the plugin needed a model config it could use).

No agent code written yet. On purpose. The car before the seats.
